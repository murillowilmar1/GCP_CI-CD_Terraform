.terraform/
# .terraform.lock.hcl se versiona a propósito (fija la versión exacta de
# cada provider por módulo/capa) — no lo ignores.
*.tfstate
*.tfstate.*
crash.log
crash.*.log
tfplan
tfplan.txt
.env
__pycache__/
